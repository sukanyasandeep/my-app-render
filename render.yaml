databases:
  - name: postgres
    ipAllowList: []
    databaseName: my-app-render_production
    user: sukanya

services:
  - type: web
    name: my-app-render
    env: ruby
    buildCommand: "./render-build.sh"
    startCommand: "bundle exec rails s"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: postgres
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false